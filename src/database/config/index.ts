// import { mysqlConfig } from './process.env.mysql';
// import { pgConfig } from './process.env.postgres';

// const databaseConfigs = {
//   mysql: mysqlConfig,
//   postgres: pgConfig,
// };
// const databaseType = process.env.DB_TYPE || 'mysql';
// const databaseConfig = databaseConfigs[databaseType];

// if (!databaseConfig) {
//   throw new Error(`Unsupported database type: ${databaseType}`);
// }
// export default databaseConfig;
